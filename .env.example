# ── Development ─────────────────────────────────────────────────────────────
# Empty base URL = Vite proxies /api → VITE_API_PROXY_TARGET
VITE_API_BASE_URL=
VITE_API_PROXY_TARGET=https://api.karamstore.ly
# VITE_API_PORT=3001

# ── Production (file: .env.production) ──────────────────────────────────────
# VITE_API_BASE_URL=https://api.karamstore.ly
# VITE_MEDIA_BASE_URL=https://api.karamstore.ly
